apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: argocd
  namespace: argocd
spec:
  entryPoints:
    - web
    - websecure
  routes:
  - match: Host(`cd.apps.lab.mxe11.nl`)
    kind: Rule
    services:
    - name: argocd-server
      port: 80
