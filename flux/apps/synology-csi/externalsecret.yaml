# Temporarily disabled until ExternalSecret CRD installed (external-secrets chart)
# ---
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: synology-csi-client-config
#   namespace: synology-csi
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     name: vault-backend
#     kind: ClusterSecretStore
#   target:
#     name: client-info-secret
#     creationPolicy: Owner
#     template:
#       engineVersion: v2
#       mergePolicy: Replace
#       type: Opaque
#       data:
#         client-info.yaml: |-
#           {{- $vals := fromYaml (toYaml .) }}
#           {{- $raw := index (append (pluck "client-info.yaml" $vals) "") 0 }}
#           {{- if $raw }}
#           {{- $raw | nindent 2 }}
#           {{- else }}
#           {{- $host := index (append (pluck "host" $vals) "") 0 }}
#           {{- $username := index (append (pluck "username" $vals) "") 0 }}
#           {{- $password := index (append (pluck "password" $vals) "") 0 }}
#           {{- if and $host $username $password }}
#           {{- $port := index (append (pluck "port" $vals) "") 0 | default 5000 }}
#           {{- $https := index (append (pluck "https" $vals) "") 0 | default "false" }}
#           clients:
#             - host: {{ $host }}
#               port: {{ $port }}
#               https: {{ $https }}
#               username: {{ $username }}
#               password: {{ $password | quote }}
#           {{- else }}
#           {{- printf "# Vault entry secret/democratic-csi/synology must provide either client-info.yaml or host/username/password keys\n" | nindent 2 }}
#           {{- end }}
#           {{- end }}
#   dataFrom:
#     - extract:
#         key: secret/democratic-csi/synology
