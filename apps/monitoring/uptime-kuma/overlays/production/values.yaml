---
# Production-specific values for Uptime Kuma
uptime-kuma:
  # Add production-specific configuration here
  volume:
    enabled: true
    accessMode: ReadWriteOnce
    size: 4Gi
    storageClassName: local-path
