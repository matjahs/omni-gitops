---
machine:
  kubelet:
    extraMounts:
      - destination: /var/lib/longhorn
        type: bind
        source: /var/lib/longhorn
        options: [bind, rshared, rw]
  kernel:
    modules:
      - name: iscsi_tcp
      - name: dm_snapshot
      - name: dm_mirror
      - name: dm_thin_pool
      - name: dm_crypt
